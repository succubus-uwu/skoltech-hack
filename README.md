Для запуска необходимо прописать: \
docker compose build \
docker compose up -d

Далее для работы с api необходимо открыть url 127.0.0.1:8777/docs. Там будет endpoint для отправки запросов

К сожалению ни у кого из нас не было опыта работы с OSM, и, как мы понимаем, мы не смогли извлечь из pbf файла все существующие там дома Москвы(сравнили официальную статистику с нашей базой, увидели, что смогли извлечь только 34 тысячи зданий из 100 тысяч, существующих в реальности). Из-за этого на некоторые запросы найденные адресы не возвращаются, хотя система отрабатывает корректно
